steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - gcloud
      - functions
      - deploy
      - traccar-combined2
      - --region=europe-west1
      - --source=.
      - --trigger-http
      - --runtime=nodejs20
